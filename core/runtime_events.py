# runtime_events.py

class RuntimeEvents:

    OBSERVE_COMPLETE = (
        "observe_complete"
    )

    CONTEXT_UPDATED = (
        "context_updated"
    )

    PLAN_CREATED = (
        "plan_created"
    )

    ACTION_EXECUTED = (
        "action_executed"
    )

    ACTION_FAILED = (
        "action_failed"
    )

    VERIFICATION_SUCCESS = (
        "verification_success"
    )

    RECOVERY_TRIGGERED = (
        "recovery_triggered"
    )

    GOAL_COMPLETED = (
        "goal_completed"
    )